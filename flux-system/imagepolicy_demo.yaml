apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: demo
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: demo
  policy:
    #alphabetical:
    # order: asc
    semver:
      range: ">1.0.0"
